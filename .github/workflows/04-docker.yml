name: Docker Build
on: workflow_call

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'zulu'

      - name: Extract Maven version
        id: project_version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "tag=$VERSION" >> $GITHUB_OUTPUT

      - name: Login to GitHub Container Registry
        uses: docker/login-action@343f7c4344506bcbf9b4de18042ae17996df046d
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@0565240e2d4ab88bba5387d719585280857eed09
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/f1-calendar:latest
            ghcr.io/${{ github.repository_owner }}/f1-calendar:${{ steps.project_version.outputs.tag }}